[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."generate-trip"]
  timeout = 30

[[redirects]]
  from = "/destinations"
  to = "/index.html"
  status = 200
